#include "main.h"
/**
* mul- function to print the multiplication of two numbers
*
*@a: first number
*@b: second number
*
*Return: the result of the multiplicaion
*/
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
